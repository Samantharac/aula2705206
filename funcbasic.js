// Objetivo de uma função  = definir um bloco de código reutilizável
// function <nome> (<parametros>){ ... }

function soma(x, y) {  // recebe dados como parâmetro
let soma = x + y; // busquem declarar apenas variáveis LET nas functions
return soma;  // retorna a informação
}

var x = soma(1, 2);
console.log(x);


function soma(x, y)
  {
      let soma = x + y;
      console.log(soma);
  }

  soma(1, 2);
  soma(3, 4);
  soma(5, 6);






/*
function soma(x, y)
  {
      let soma = x + y;
      console.log(soma);
  }

  soma(1, 2);
  soma(3, 4);
  soma(5, 6);
*/
// Arquivo criado em projeto no sitio denominado funcbasica.js